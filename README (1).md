# Model Directory

Please download the fine-tuned Phi-2 GGUF model and place it in this directory.

**Expected Filename:** `phi-2-insurance.gguf`

If you use a different name, make sure to update the `model_path` in `../utils/model_handler.py`.
